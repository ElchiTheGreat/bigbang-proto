syntax = "proto3";

package client;

option go_package = ".;client";

// Deploy related messages
message RequestDeploy {
  string name = 1;
  string ipv4 = 2;
  uint32 port = 3;
  bytes bootstrap = 4;
}
